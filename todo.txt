Add support for multiples. Examples: a().b().c(). complete
Add string example: "hola mundo". complete
Add string concat,multiples methods
Add array example: [1, 2, 3, 4]. complete
Add types example: var a: int = 123.
Add support for changing scope for multiples (global scope, function scope, curly brace scope).
Add HTTP library.
Add file system library.
Add import and export functionality.
Add WebAssembly support.
Refactor error display.
Add dynamic error display in conf.json.
Add optional parentheses in conf.json.
Use different tokens for assignment (=) and variable declaration (var =).
Make var optional for creating variables.
Enable and disable const.
In conf.json, add option to start with (main function, main class, or script).
Add option in conf.json for event loop or thread.
Enable CLI execution.